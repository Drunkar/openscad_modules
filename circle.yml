machine:
    timezone: Asia/Tokyo

dependencies:
    pre:
        - sudo apt-get update
        - sudo apt-get install openscad

test:
    post:
        - openscad -o ${CIRCLE_ARTIFACTS}/ArcOfCylinder.stl modules/ArcOfCylinder.scad