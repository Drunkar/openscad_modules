machine:
    timezone: Asia/Tokyo

dependencies:
    pre:
        - sudo apt-get update
        - sudo apt-get install openscad
        - pip install -r configfile/requirements-dev.txt

test:
    post:
        - invoke test