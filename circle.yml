machine:
    timezone: Asia/Tokyo

dependencies:
    pre:
        - sudo apt-get update
        - sudo apt-get install openscad

test:
    post:
        - openscad -o ArcOfCylinder.stl modules/ArcOfCylinder.scad