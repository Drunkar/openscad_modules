language: python
 
python:
- "3.4"

machine:
    timezone: Asia/Tokyo

dependencies:
    pre:
        - sudo apt-get update
        - sudo apt-get install openscad
        - sudo pip install --upgrade pip
        - sudo pip install -r requirements-dev.txt

test:
    post:
        - invoke test