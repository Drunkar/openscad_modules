general:
    branches:
        ignore:
            - images

machine:
    timezone: Asia/Tokyo
    python:
        version: 3.5.1

dependencies:
    pre:
        - curl -o- https://launchpad.net/~openscad/+archive/ubuntu/releases/+build/7368891/+files/openscad_2015.03-1+dfsg-0.1~trusty1_amd64.deb | dpkg --install 
        - sudo apt-get update
        - sudo apt-get install openscad
        - sudo pip install --upgrade pip
        - sudo pip install -r requirements-dev.txt

test:
    post:
        - invoke test